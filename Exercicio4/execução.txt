Criar arquivo .env e configurar as variaveis de ambiente

docker-compose up -d 

docker exec -ti mysql bash

mysql -u root -p
--> Entrar com Senha

use banco;

create table pessoas (
    id int auto_increment primary key,
    nome varchar(100) not null,
    cpf int not null
);

insert into pessoas (nome, cpf) values ("pessoa", 12345);

exit //Sair do container

docker volume ls //Verifica o volume

docker-compose down //Remove o container e a network

docker-compose up -d //Cria somente o container e a network

docker exec -ti mysql bash

mysql -u root -p
--> Entrar com Senha

use banco;

select * from pessoas; //Verifica os dados




